diff --git a/package.json b/package.json
index 408332c..8c3c57d 100644
--- a/package.json
+++ b/package.json
@@ -14,10 +14,7 @@
     },
     "devDependencies" : {
         "mocha" : ">=1.0.0",
-        "canvas" : ">=0.10.0",
-        "cradle" : ">=0.2.0",
-        "should" : ">=0.6.0",
-        "async" : ">=0.1.18"
+        "canvas" : ">=0.10.0"
     },
     "keywords": ["neural network", "classifier", "machine learning"]
 }
\ No newline at end of file
diff --git a/test/cross-validation/color-contrast.js b/test/cross-validation/color-contrast.js
deleted file mode 100644
index e0c98c6..0000000
--- a/test/cross-validation/color-contrast.js
+++ /dev/null
@@ -1,62 +0,0 @@
-var url = require("url"),
-    assert = require("should"),
-    cradle = require("cradle"),
-    _ = require("underscore"),
-    brain = require("../../lib/brain"),
-    crossValidate = require("../../lib/cross-validate");
-
-function getData(couchUrl, callback) {
-  var parts = url.parse(couchUrl);
-
-  var client = new cradle.Connection(parts.hostname, parts.port || 80);
-  var db = client.database(parts.pathname.replace(/^\//, ""));
-
-
-  db.all({include_docs: true}, function(err, res) {
-    if (err) {
-      console.log("error getting data from " + url + ": ");
-      console.log(err);
-    }
-    else {
-      var data = _(res.rows).pluck("doc");
-      callback(data);
-    }
-  });
-}
-
-function runTest(url, callback) {
-  getData(url, function(data) {
-    var result = crossValidate(brain.NeuralNetwork, {}, data);
-    callback(result);
-  });
-}
-
-describe('neural network cross-validation', function() {
-  it('learn best text color for a background color', function(done) {
-    var couchUrl = "http://harth.iriscouch.com/blackorwhite";
-
-    runTest(couchUrl, function(result) {
-      console.log("\nMisclassifications:");
-      result.misclasses.forEach(function(misclass) {
-        console.log("input: " + misclass.input
-          + " actual: " + letters[misclass.actual]
-          + " expected: " + letters[misclass.expected] + "\n")
-      })
-
-      console.log("Cross-validation of color contrast data:\n");
-      console.log(result.avgs);
-
-      console.log("\nMisclassification rate: "
-      + result.misclasses.length / (result.avgs.testSize + result.avgs.trainSize));
-
-      console.log("\nMean squared error: "
-      + result.avgs.error);
-
-      var perf = result.avgs.iterations / (result.avgs.trainTime / 1000);
-      console.log("\nTraining iterations per second: " + perf);
-
-      assert.ok(result.avgs.error < .02);
-      done();
-    })
-  })
-})
diff --git a/test/cross-validation/ocr.js b/test/cross-validation/ocr.js
index 0db7045..308085a 100644
--- a/test/cross-validation/ocr.js
+++ b/test/cross-validation/ocr.js
@@ -1,5 +1,4 @@
-var assert = require("should"),
-    canvas = require("canvas"),
+var canvas = require("canvas"),
     _ = require("underscore"),
     brain = require("../../lib/brain"),
     crossValidate = require("../../lib/cross-validate");
@@ -60,7 +59,8 @@ describe('OCR cross-validation', function() {
        });
     });
 
-    var result = crossValidate(brain.NeuralNetwork, {}, data);
+    console.log("Cross validating");
+    var result = crossValidate(brain.NeuralNetwork, data, {});
 
     console.log("\nMisclassifications:");
     result.misclasses.forEach(function(misclass) {
diff --git a/test/unit/bitwise.js b/test/unit/bitwise.js
index 3944890..2efdff1 100644
--- a/test/unit/bitwise.js
+++ b/test/unit/bitwise.js
@@ -1,4 +1,4 @@
-var assert = require('should'),
+var assert = require("assert"),
     brain = require("../../lib/brain");
 
 var wiggle = 0.1;
diff --git a/test/unit/hash.js b/test/unit/hash.js
index 8fe4270..c1fffd6 100644
--- a/test/unit/hash.js
+++ b/test/unit/hash.js
@@ -1,4 +1,4 @@
-var assert = require('should'),
+var assert = require("assert"),
     brain = require("../../lib/brain");
 
 describe('hash input and output', function() {
diff --git a/test/unit/json.js b/test/unit/json.js
index 25dc19c..aadc01a 100644
--- a/test/unit/json.js
+++ b/test/unit/json.js
@@ -1,4 +1,4 @@
-var assert = require('should'),
+var assert = require("assert"),
     brain = require("../../lib/brain");
 
 describe('JSON', function() {
diff --git a/test/unit/lookup.js b/test/unit/lookup.js
index 2f83e78..c0ec87a 100644
--- a/test/unit/lookup.js
+++ b/test/unit/lookup.js
@@ -1,4 +1,4 @@
-var assert = require('should'),
+var assert = require("assert"),
     lookup = require("../../lib/lookup");
 
 
diff --git a/test/unit/options.js b/test/unit/options.js
index 7884dd6..fcf1332 100644
--- a/test/unit/options.js
+++ b/test/unit/options.js
@@ -1,4 +1,4 @@
-var assert = require('should'),
+var assert = require("assert"),
     _ = require("underscore"),
     brain = require("../../lib/brain");
 
diff --git a/test/unit/stream-bitwise.js b/test/unit/stream-bitwise.js
index a54d5f1..803fecf 100644
--- a/test/unit/stream-bitwise.js
+++ b/test/unit/stream-bitwise.js
@@ -1,5 +1,5 @@
-var assert = require('should'),
-  brain = require("../../lib/brain");
+var assert = require("assert"),
+    brain = require("../../lib/brain");
 
 function StreamTester(opts) {
   if (!(this instanceof StreamTester)) return new StreamTester(opts);
diff --git a/test/unit/trainopts.js b/test/unit/trainopts.js
index 3394d76..5ca84e7 100644
--- a/test/unit/trainopts.js
+++ b/test/unit/trainopts.js
@@ -1,4 +1,4 @@
-var assert = require("should"),
+var assert = require("assert"),
     brain = require("../../lib/brain");
 
 var data = [{input: [0, 0], output: [0]},
